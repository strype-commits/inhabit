// Copy to secrets.h (gitignored) and fill in. Never commit real values.
#pragma once

// WiFi networks, tried in turn (strongest available wins).
const char* wifi_ssids[]     = { "Network 1", "Network 2" };
const char* wifi_passwords[] = { "password1", "password2" };
const int wifi_count = 2;

#define DATABASE_URL "https://inhabit-webapp-default-rtdb.europe-west1.firebasedatabase.app"
#define OTA_BASE_URL "https://inhabit-firmware.web.app"

// Auth option 1 (preferred): a Firebase Auth account for this device.
#define FIREBASE_API_KEY ""   // Firebase web API key (public)
#define DEVICE_EMAIL ""       // leave empty to use option 2
#define DEVICE_PASSWORD ""

// Auth option 2 (legacy): the database secret. Bypasses all security rules.
#define FIREBASE_LEGACY_TOKEN ""
