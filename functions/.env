# Non-secret settings for Cloud Functions (secrets use `firebase functions:secrets:set`).
# Sender must be on a domain verified in Resend.
ALERT_EMAIL_FROM="inHabit <alerts@strype.uk>"
# Links in alert emails. Switch to the production URL at cutover.
APP_URL="https://inhabit-webapp-dev.web.app"
